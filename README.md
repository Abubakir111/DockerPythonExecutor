# Python Code Executor in Docker

Этот проект предоставляет HTTP-сервис для выполнения кода на Python. Он работает в Docker-контейнере.

## Как запустить

1. Соберите Docker-образ:
   ```bash
   docker build -t flask-executor .
   ```
